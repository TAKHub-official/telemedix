# TeleMedix Umgebungsvariablen
# Kopieren Sie diese Datei zu .env und passen Sie die Werte an

# Allgemeine Einstellungen
NODE_ENV=development  # Ändern zu 'production' für Produktionsumgebungen
PORT=3000
FRONTEND_URL=http://localhost:5173  # Ändern für Produktion

# JWT-Authentifizierung
JWT_SECRET=change_this_to_a_secure_random_string_in_production
JWT_EXPIRATION=1d

# PostgreSQL-Datenbank
DB_PASSWORD=password  # Ändern für Produktion
DATABASE_URL=postgresql://postgres:${DB_PASSWORD}@db:5432/telemedix

# E-Mail-Konfiguration (optional)
# EMAIL_HOST=smtp.beispiel.de
# EMAIL_PORT=587
# EMAIL_USER=benachrichtigungen@beispiel.de
# EMAIL_PASS=email_passwort
# EMAIL_FROM=telemedix@beispiel.de

# Telegram-Bot-Konfiguration (optional)
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token 